<%= form_with(model: room_night) do |form| %>
  <% if room_night.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room_night.errors.count, "error") %> prohibited this room_night from being saved:</h2>

      <ul>
        <% room_night.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :room_id, style: "display: block" %>
    <%= form.text_field :room_id %>
  </div>

  <div>
    <%= form.label :booking_id, style: "display: block" %>
    <%= form.text_field :booking_id %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
