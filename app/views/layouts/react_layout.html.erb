<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Rails React" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
</head>

<body data-turbo="false" class="relative">
<%= render "nav", show_data_reset: true %>
<div class="container container-lg mx-auto px-4 pt-8">
  <div><%= content_for(:breadcrumbs) %></div>
  <%= yield %>
</div>
<div id="overlay" class="w-dvh h-dvh inset-0 absolute bg-white visible transition-all duration-500"></div>
</body>
</html>
